streamlit>=1.32.0
pandas>=2.0.0
openpyxl>=3.1.0
plotly>=5.18.0
# AI Assistant uses the Anthropic API via stdlib urllib — no extra package required.
# The API key is injected by Streamlit Cloud secrets (see README for setup).
